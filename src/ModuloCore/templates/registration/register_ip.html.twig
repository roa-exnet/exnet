{# templates/registration/register_ip.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}ExNet | Registro Rápido por IP{% endblock %}

{% block stylesheets %}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="{{ asset('css/roa-auth.css') }}">
    <style>
        .ip-info {
            background-color: rgba(0, 0, 0, 0.6);
            padding: 10px;
            border-radius: 5px;
            margin-bottom: 15px;
            color: white;
        }
        .ip-info .ip-address {
            font-weight: bold;
            color: #ffcc00;
        }
    </style>
{% endblock %}

{% block javascripts %}
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow-sm roa-card">
                <div class="card-header bg-primary text-white roa-card-header">
                    <h2 class="h4 mb-0">Registro Rápido por IP</h2>
                </div>
                <div class="card-body p-4 roa-card-body">
                    <div class="ip-info">
                        <p>Tu dirección IP <span class="ip-address">{{ currentIp }}</span> no está registrada en EXNET.</p>
                        <p>Completa este formulario para acceso por IP privada (VPN).</p>
                    </div>

                    {% if errors is defined and errors %}
                        <div class="alert alert-danger roa-alert roa-alert-danger">
                            <ul class="mb-0">
                                {% for error in errors %}
                                    <li>{{ error }}</li>
                                {% endfor %}
                            </ul>
                        </div>
                    {% endif %}

                    <form method="post">
                        <div class="mb-3 roa-form-group">
                            <label for="email" class="form-label roa-form-label">Email</label>
                            <input type="email" name="email" id="email" class="form-control roa-form-control" 
                                   value="{{ email|default('') }}" required autofocus>
                        </div>
                        
                        <div class="mb-3 roa-form-group">
                            <label for="nombre" class="form-label roa-form-label">Nombre</label>
                            <input type="text" name="nombre" id="nombre" class="form-control roa-form-control" 
                                   value="{{ nombre|default('') }}" required>
                        </div>
                        
                        <div class="mb-3 roa-form-group">
                            <label for="apellidos" class="form-label roa-form-label">Apellidos</label>
                            <input type="text" name="apellidos" id="apellidos" class="form-control roa-form-control" 
                                   value="{{ apellidos|default('') }}" required>
                        </div>

                        <button class="btn btn-primary w-100 roa-btn roa-btn-primary roa-btn-block" type="submit">
                            Registrarse
                        </button>
                    </form>

                    <div class="mt-4 text-center roa-text-center roa-mt-3">
                        <p class="small text-muted">
                            Al registrarte, aceptas que tu dirección IP {{ currentIp }} será asociada a tu cuenta 
                            para permitir el acceso automático.
                        </p>
                        <p>¿Ya tienes cuenta? <a href="{{ path('app_login') }}">Inicia sesión</a></p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}